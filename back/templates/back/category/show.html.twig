{% extends 'base.html.twig' %}

{% block title %}Category{% endblock %}

{% block body %}
   

     

    <table class="table table-hover">
        <tbody>
            <tr class="table-info">
                <th>Id</th>
                <td>{{ category.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ category.name }}</td>
            </tr>
        </tbody>
    </table>

     <a href="{{ path('back_category_index') }}" class="btn btn-outline-info">back to list</a>

    <a href="{{ path('back_category_edit', {'id': category.id}) }}" class="btn btn-outline-primary">edit</a>

     {{ include('back/category/_delete_form.html.twig') }} 
{% endblock %}
