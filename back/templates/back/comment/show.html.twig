{% extends 'base.html.twig' %}

{% block title %}Comment{% endblock %}

{% block body %}


    <table class="table table-hover">
        <tbody>
            <tr class="table-info">
                <th>Id</th>
                <td>{{ comment.id }}</td>
            </tr>
            <tr>
                <th>Content</th>
                <td>{{ comment.content }}</td>
            </tr>
            <tr>
                <th>Post</th>
                <td>{{ comment.post}}</td>
            </tr>
             <tr>
                <th>User</th>
                <td>{{ comment.user}}</td>
            </tr>
            <tr>
                <th>CreatedAt</th>
                <td>{{ comment.createdAt ? comment.createdAt|date('Y-m-d H:i') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('back_comment_index') }}" class="btn btn-outline-info">back to list</a>

    <a href="{{ path('back_comment_edit', {'id': comment.id}) }}" class="btn btn-outline-primary">edit</a>

     {{ include('back/comment/_delete_form.html.twig') }} 
{% endblock %}
